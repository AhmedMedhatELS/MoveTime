@page
@model PersonalDataModel
@{
    ViewData["Title"] = "البيانات الشخصية"; // Title in Arabic
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-8 offset-md-2">
        <!-- Center the content -->
        <div class="card shadow-sm">
            <!-- Added card for better design -->
            <div class="card-body">
                <p>تحتوي حسابك على بيانات شخصية قدمتها لنا. تتيح لك هذه الصفحة تنزيل هذه البيانات أو حذفها.</p>
                <p>
                    <strong>سيؤدي حذف هذه البيانات إلى إزالة حسابك نهائيًا، ولا يمكن استرداده.</strong>
                </p>
                <div class="d-flex justify-content-between">
                    <!-- Flexbox for alignment -->
                    <form id="download-data" asp-page="DownloadPersonalData" method="post">
                        <button class="btn btn-primary" type="submit">تنزيل</button> <!-- Download button in Arabic -->
                    </form>
                    <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">حذف</a> <!-- Delete button in Arabic -->
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
